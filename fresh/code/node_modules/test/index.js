var Mod = require("mod42/v1a");
var is = require("util42/is");

var Tests = module.exports = Mod.extend({
	name: "Tests",
	log: {
		log: true,
		excludes: ["log", "hasOwnProperty", "assert"],
		methods: {
			expand: true,
			ctx: false
		},
		method: {
			instantiate: false,
			initialize: false,
			run: false,
			set: false
		}
	},
	initialize: function(){
		console.group(this.constructor.name);
		this.run();
		console.groupEnd();
	},
	assert: console.assert.bind(console),
	run: function(){}
});